import UuSprintmanToolUu5lib from "uu-sprintman-tool-uu5lib";
import { mount, wait } from "uu5g05-test";

describe(`UuSprintmanToolUu5lib.Bricks.TicketImporter`, () => {
  // it("onLoad", async () => {
  //   let onLoad = jest.fn(async () => ({ content: "Hello, world!" }));
  //   let loadDtoIn = { foo: "bar" };
  //   let wrapper = mount(<UuSprintmanToolUu5lib.Bricks.TicketImporter onLoad={onLoad} loadDtoIn={loadDtoIn} />);
  //   expect(onLoad).toHaveBeenCalledTimes(1);
  //   expect(onLoad).lastCalledWith(loadDtoIn);
  //   await wait();
  //   expect(wrapper.text()).toContain("Hello, world!");
  // });
});
